# Box

A titled container on `panel` for the relationship's side column: Log, Promises, Next meeting.

- The title is a mono label. Boxes stack with `space-3` between them and never nest.
- Consumer provides `title` and children.
