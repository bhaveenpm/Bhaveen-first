# Eyebrow

The mono line above a selected action that names its reason and its group: "Promise · Late".

- Always pair it with the action's `kind` so the mark and the words agree.
- `late` sets the whole line in `late`; use it only for the Late group.
- Consumer provides the words. Keep them to the reason and the group, separated by a middle dot.
