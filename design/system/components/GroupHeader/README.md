# GroupHeader

The label and count above each group in the queue: Late, Today, This week, Going quiet, With them.

- The groups always appear in that order and an empty group is left out.
- `late` sets the Late header in `late`. No other header is coloured.
