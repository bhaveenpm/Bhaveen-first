# Key

A key cap: the letter that performs an action from the keyboard.

- Show it wherever a shortcut exists: inside a KeyButton, and in the KeyLegend at the foot of the desk.
- One character or a symbol (`E`, `S`, `U`, `J`, `K`, `↵`). Never a word.
