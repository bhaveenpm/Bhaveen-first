# Segmented

Two or three mutually exclusive views of the same list, such as Mine and Studio.

- Use it for scope, never for navigation between screens.
- Labels are one word each. The pressed segment is `raise` with `ink` text.
- Consumer provides `options`, the current `value` and `onChange`.
